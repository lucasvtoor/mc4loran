using System.Numerics;
using Managers;
using Settings;

namespace World;

public class PlainsGrassLayer(SettingsManager settingsManager, Vector3 bottomY) : Generator(settingsManager, bottomY)
{
    public override void Generate()
    {
        throw new NotImplementedException();
    }
}